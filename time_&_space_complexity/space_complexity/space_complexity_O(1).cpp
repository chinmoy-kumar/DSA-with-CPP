// Space complexity depends on array

#include <bits/stdc++.h>
#include <iostream>
using namespace std;
int main(){
    int n; // O(1)
    cin >> n; 
    for (int i = 0; i < n; i++) // O(1)
    {
        cout << i << endl;
    }

    return 0;
}