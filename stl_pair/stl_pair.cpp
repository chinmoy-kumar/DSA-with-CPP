#include <bits/stdc++.h>
using namespace std;
int main(){
    pair<string,int> p;
    // p = make_pair("Hello", 3);
    // or, shortcut: 
    p = {"Hello", 3};

    // print the first value
    cout << p.first << endl;

    // print the second value
    cout << p.second << endl;

    return 0;
}